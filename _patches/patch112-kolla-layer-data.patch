commit d6f9c65c790329c3158a17548ac3ed89acb025cf
Author: Michael Still <mikal@stillhq.com>
Date:   Tue Dec 16 09:41:07 2025 +1100

    Track the layers created for various images and their sizes.
    
    Change-Id: I21c4e6209fc4f29a8d58938b7c7a0d4f614f3484
    Signed-off-by: Michael Still <mikal@stillhq.com>

diff --git a/kolla/common/config.py b/kolla/common/config.py
index 653c84c53..ea9b8de41 100644
--- a/kolla/common/config.py
+++ b/kolla/common/config.py
@@ -271,7 +271,9 @@ _CLI_OPTS = [
     cfg.StrOpt('engine', default='docker', choices=['docker', 'podman'],
                help='Container engine to build images on.'),
     cfg.StrOpt('podman_base_url', default='unix:///run/podman/podman.sock',
-               help='Path to podman socket.')
+               help='Path to podman socket.'),
+    cfg.StrOpt('layer-json-file',
+               help='Emit information about the layers in each built image.')
 ]
 
 _BASE_OPTS = [
diff --git a/kolla/image/kolla_worker.py b/kolla/image/kolla_worker.py
index 723914f5a..353642c80 100644
--- a/kolla/image/kolla_worker.py
+++ b/kolla/image/kolla_worker.py
@@ -57,6 +57,7 @@ class Image(object):
         self.plugins = []
         self.additions = []
         self.engine_client = engine_client
+        self.layer_data = []
 
     def copy(self):
         c = Image(self.name, self.canonical_name, self.path,
@@ -621,6 +622,26 @@ class KollaWorker(object):
                 # backwards-compatibility.
                 json_summary(sys.stdout)
 
+        if self.conf.layer_json_file:
+            layer_info = []
+            for image in self.images:
+                if image.layer_data:
+                    layer_info.append({
+                        'name': image.canonical_name,
+                        'status': str(image.status),
+                        'layers': image.layer_data
+                    })
+
+            try:
+                with open(self.conf.layer_json_file, 'w') as f:
+                    f.write(
+                        json.dumps(layer_info, indent=4, sort_keys=True))
+            except OSError as e:
+                LOG.error('Failed to write JSON layer summary to '
+                          f'{self.conf.layer_json_file}')
+                LOG.error(f'Exception caught: {e}')
+                sys.exit(1)
+
         return results
 
     def get_image_statuses(self):
diff --git a/kolla/image/tasks.py b/kolla/image/tasks.py
index 39d759390..17dd8c804 100644
--- a/kolla/image/tasks.py
+++ b/kolla/image/tasks.py
@@ -475,6 +475,17 @@ class BuildTask(EngineTask):
             self.logger.info('Built at %s (took %s)' %
                              (now, now - image.start))
 
+            if self.conf.layer_json_file:
+                try:
+                    built_image = self.engine_client.images.get(
+                        image.canonical_name)
+                    image.layer_data = built_image.history()
+
+                except requests.exceptions.HTTPError as e:
+                    self.logger.exception(
+                        'Unexpected error while fetching image layer '
+                        'data: %s', e)
+
     def squash(self):
         image_tag = self.image.canonical_name
         image_id = self.engine_client.images.get(image_tag).id
