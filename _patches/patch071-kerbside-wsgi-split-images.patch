commit 35c0f2e19189f8fba69b885dd7f41457b30f417a
Author: Michael Still <mikal@stillhq.com>
Date:   Sun Aug 31 13:53:43 2025 +1000

    Add a kerbside API image.
    
    Signed-off-by: Michael Still <mikal@stillhq.com>

diff --git a/docker/kerbside/kerbside-api/Dockerfile.j2 b/docker/kerbside/kerbside-api/Dockerfile.j2
new file mode 100644
index 0000000..4e5e1f7
--- /dev/null
+++ b/docker/kerbside/kerbside-api/Dockerfile.j2
@@ -0,0 +1,9 @@
+FROM {{ namespace }}/{{ image_prefix }}kerbside-base:{{ tag }}
+{% block labels %}
+LABEL maintainer="{{ maintainer }}" name="{{ image_name }}" build-date="{{ build_date }}"
+{% endblock %}
+
+COPY extend_start.sh /usr/local/bin/kolla_extend_start
+RUN chmod 644 /usr/local/bin/kolla_extend_start
+
+USER kerbside
diff --git a/docker/kerbside/kerbside-api/extend_start.sh b/docker/kerbside/kerbside-api/extend_start.sh
new file mode 100644
index 0000000..48b2af6
--- /dev/null
+++ b/docker/kerbside/kerbside-api/extend_start.sh
@@ -0,0 +1,13 @@
+#!/bin/bash
+set -o errexit
+
+# Make the log directory
+mkdir -p /var/log/kolla/kerbside
+
+# Load config from environment variables
+. /etc/kerbside/kerbside_config
+
+# Bootstrapping is done with the kerbside-proxy image, not this
+# one.
+
+. /usr/local/bin/kolla_httpd_setup
diff --git a/docker/kerbside/kerbside/Dockerfile.j2 b/docker/kerbside/kerbside-base/Dockerfile.j2
similarity index 79%
rename from docker/kerbside/kerbside/Dockerfile.j2
rename to docker/kerbside/kerbside-base/Dockerfile.j2
index 84154b2..6ede949 100644
--- a/docker/kerbside/kerbside/Dockerfile.j2
+++ b/docker/kerbside/kerbside-base/Dockerfile.j2
@@ -8,6 +8,8 @@ LABEL maintainer="{{ maintainer }}" name="{{ image_name }}" build-date="{{ build
 {% import "macros.j2" as macros with context %}
 {{ macros.configure_user(name='kerbside', groups='kerbside') }}
 
+{{ macros.configure_user(name='kerbside', groups='kerbside') }}
+
 {% if base_package_type == 'rpm' %}
     {% set kerbside_packages = [
         'mariadb-devel',
@@ -25,24 +27,25 @@ LABEL maintainer="{{ maintainer }}" name="{{ image_name }}" build-date="{{ build
 # merged upstream. This is handled as a plugin for now. This is also how you
 # would add support to kerbside for other clouds like Shaken Fist or oVirt.
 ADD plugins-archive /
-RUN pip3 --no-cache-dir install /plugins/*
 
 {% set kerbside_pip_packages = [
     '/kerbside'
 ] %}
 
+{% set kerbside_plugins_pip_packages = [
+    '/plugins/*'
+] %}
+
 # Note that we disable upper-constraints for kerbside, because it comes from an
 # alternate universe which requires more modern dependencies that you get with
-# 2023.1. This should change as OpenStack releases newer versions.
-ADD kerbside-archive /kerbside-source
+# 2025.1. This should change as OpenStack releases newer versions.
+ADD kerbside-base-archive /kerbside-source
 RUN ln -s kerbside-source/* kerbside \
+    && if [ "$(ls /plugins)" ]; then \
+           {{ macros.install_pip(kerbside_plugins_pip_packages) }}; \
+       fi \
     && {{ macros.install_pip(kerbside_pip_packages | customizable("pip_packages"), false) }} \
     && rm -f /etc/machine-id
 
-COPY extend_start.sh /usr/local/bin/kolla_extend_start
-RUN chmod 644 /usr/local/bin/kolla_extend_start
-
 {% block kerbside_footer %}{% endblock %}
 {% block footer %}{% endblock %}
-
-USER kerbside
diff --git a/docker/kerbside/kerbside-proxy/Dockerfile.j2 b/docker/kerbside/kerbside-proxy/Dockerfile.j2
new file mode 100644
index 0000000..4e5e1f7
--- /dev/null
+++ b/docker/kerbside/kerbside-proxy/Dockerfile.j2
@@ -0,0 +1,9 @@
+FROM {{ namespace }}/{{ image_prefix }}kerbside-base:{{ tag }}
+{% block labels %}
+LABEL maintainer="{{ maintainer }}" name="{{ image_name }}" build-date="{{ build_date }}"
+{% endblock %}
+
+COPY extend_start.sh /usr/local/bin/kolla_extend_start
+RUN chmod 644 /usr/local/bin/kolla_extend_start
+
+USER kerbside
diff --git a/docker/kerbside/kerbside/extend_start.sh b/docker/kerbside/kerbside-proxy/extend_start.sh
similarity index 100%
rename from docker/kerbside/kerbside/extend_start.sh
rename to docker/kerbside/kerbside-proxy/extend_start.sh
diff --git a/kolla/common/sources.py b/kolla/common/sources.py
index 309da11..8f36810 100644
--- a/kolla/common/sources.py
+++ b/kolla/common/sources.py
@@ -164,7 +164,7 @@ SOURCES = {
         'type': 'url',
         'location': ('$tarballs_base/openstack/keystone/'
                      'keystone-${openstack_branch}.tar.gz')},
-    'kerbside': {
+    'kerbside-base': {
         'type': 'url',
         'location': ('https://shakenfist.com/kerbside/releases/'
                      'kerbside-${openstack_branch}.tar.gz')},
