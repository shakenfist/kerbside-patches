name: Ensure the cloud is still installable

on:
  workflow_dispatch:
  pull_request:
    branches:
      - develop

jobs:
  build_images:
    name: "Build docker images for ${{ matrix.test.release }}"
    strategy:
      fail-fast: false
      matrix:
        test: [
          {
            'name': 'build-2025.1',
            'release': '2025.1',
            'base': 'debian:12',
            'baseuser': 'debian'
          },
          {
            'name': 'build-master',
            'release': 'master',
            'base': 'debian:12',
            'baseuser': 'debian',
            'topology': 'all-in-one'
          },
        ]
    runs-on: [self-hosted, vm, 'debian-12', 'xl']
    timeout-minutes: 120
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}-${{ matrix.test.name }}
      cancel-in-progress: true

    # NOTE(mikal): git repos are checked out to /srv/github/_work/{org}/{repo}
    # which is available as GITHUB_WORKSPACE. You can find other environment
    # variables at https://docs.github.com/en/actions/learn-github-actions/environment-variables

    steps:
      - name: Checkout kerbside-patches
        uses: actions/checkout@v4
        with:
          path: kerbside-patches
          fetch-depth: 0

      - name: Install dependencies on local runner
        run: |
          . $GITHUB_ENV
          cd kerbside-patches

          ./_build/install-build-dependencies.sh
          ./_build/setup-local-build-environment.sh
          ./_build/add-insecure-ci-registry.sh

          # This is terrible, but its easier than reloading groups
          sudo chmod ugo+rw /var/run/docker.sock

      - name: Build patched repositories
        run: |
          . $GITHUB_ENV
          cd kerbside-patches
          ./_build/assemble-source.sh ${{ matrix.test.release }}

      - name: Build container images
        run: |
          . $GITHUB_ENV
          cd kerbside-patches

          hash=$(./_build/calculate-container-hash.sh \
              kolla kolla-2* \
              nova nova-2* \
              requirements requirements-2* \
              src )

          ./_build/build-containers.sh \
              --use-ci-registry \
              --build-targets "${{ matrix.test.release }}" \
              --build-images all \
              --image-tag "${hash-debian-bookworm}" \
              --dont-fetch-images

      - name: Archive patched source
        if: always()
        run: |
          . $GITHUB_ENV
          cd kerbside-patches

          mkdir /tmp/bundle
          mv src/*.tgz /tmp/bundle/
          cd /tmp
          zip -r bundle.zip bundle

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: source-${{ matrix.test.release }}
          retention-days: 90
          if-no-files-found: error
          path: /tmp/bundle.zip

  functional_matrix:
    name: "Build a test cloud for ${{ matrix.test.description }}"
    strategy:
      fail-fast: false
      matrix:
        test: [
          {
            'name': '2025.1-debian12-aio',
            'description': 'OpenStack 2025.1 on Debian 12 all-in-one',
            'release': '2025.1',
            'base': 'debian:12',
            'baseuser': 'debian',
            'topology': 'all-in-one',
            'runs_on': 'debian-11'
          },
          {
            'name': 'master-debian12-aio',
            'description': 'OpenStack master on Debian 12 all-in-one',
            'release': 'master',
            'base': 'debian:12',
            'baseuser': 'debian',
            'topology': 'all-in-one',
            'runs_on': 'debian-11'
          },
          {
            'name': 'master-rocky10-aio',
            'description': 'OpenStack master on Rocky 10 all-in-one',
            'release': 'master',
            'base': 'rocky:10',
            'baseuser': 'cloud-user',
            'topology': 'all-in-one',
            'runs_on': 'debian-12'
          },
          {
            'name': 'master-debian12-multi',
            'description': 'OpenStack master on Debian 12 multinode',
            'release': 'master',
            'base': 'debian:12',
            'baseuser': 'debian',
            'topology': 'multinode',
            'runs_on': 'debian-11'
          },
        ]
    runs-on: [self-hosted, vm, '${{ matrix.test.runs_on }}']
    needs: build_images
    timeout-minutes: 120
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}-${{ matrix.test.name }}
      cancel-in-progress: true

    # NOTE(mikal): git repos are checked out to /srv/github/_work/{org}/{repo}
    # which is available as GITHUB_WORKSPACE. You can find other environment
    # variables at https://docs.github.com/en/actions/learn-github-actions/environment-variables

    steps:
      - name: Setup test environment
        uses: shakenfist/actions/setup-kerbside-environment@main
        with:
          base: ${{ matrix.test.base }}
          base_user: ${{ matrix.test.baseuser }}
          openstack_release: ${{ matrix.test.release }}
          topology: ${{ matrix.test.topology }}

      - name: Dump CI environment details
        run: |
          cat ci-environment.sh

      - name: Build patched repositories
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'cd /srv/shakenfist/kerbside-patches; ./_build/assemble-source.sh ${{ matrix.test.release }}'

      - name: Build container images
        run: |
          . $GITHUB_ENV

          script="cd /srv/shakenfist/kerbside-patches;"
          script="${script} hash=\$(./_build/calculate-container-hash.sh kolla kolla-2* nova nova-2* requirements requirements-2*);"
          script="${script} ./_build/build-containers.sh --use-ci-registry --build-targets ${{ matrix.test.release }} --build-images all --image-tag "\$hash-debian-bookworm";"

          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 "${script}"

      # - name: Disable /etc/hosts for name resolution
      #   run: |
      #     . $GITHUB_ENV
      #     ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
      #         -o UserKnownHostsFile=/dev/null \
      #         ${{ matrix.test.baseuser }}@10.0.2.2 \
      #         'sudo sed -i s"|hosts:      files dns myhostname|hosts:      dns myhostname|" /etc/nsswitch.conf'

      - name: Log getent resolution for kolla
        if: "contains(matrix.test.topology, 'all-in-one')"
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo getent ahostsv4 kolla'

      - name: Bootstrap Kolla-Ansible
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'cd /srv/shakenfist/kerbside-patches; sudo ./tools/bootstrap-kolla-ansible ${{ matrix.test.release }} ${{ matrix.test.topology }}'

      - name: Install Kolla-Ansible
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'cd /srv/shakenfist/kerbside-patches; sudo ./tools/install-kolla-ansible'

      - name: Install patched OpenStack clients
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'cd /srv/shakenfist/kerbside-patches; sudo ./tools/install-openstack-clients'

      - name: Dump list of configured haproxy services
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo ls -l /etc/kolla/haproxy/services.d/ || true'

      - name: Dump haproxy kerbside API configuration
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo cat /etc/kolla/haproxy/services.d/kerbside_api.cfg || true'

      - name: Dump haproxy kerbside proxy configuration
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo cat /etc/kolla/haproxy/services.d/kerbside_proxy.cfg || true'

      - name: Dump internal VIP CA cert details
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'openssl s_client -showcerts -connect 10.0.2.3:5000'

      - name: Dump CA cert bundle
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo openssl storeutl -noout -text -certs /etc/kolla/certificates/ca/root.crt'

      - name: Post install Kolla-Ansible setup
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'cd /srv/shakenfist/kerbside-patches; sudo ./tools/postinstall-kolla-ansible --use-ci-registry'

      - name: Verify that the haproxy is configured correctly for the Kerbside API
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'curl -v -k https://10.0.2.3:13002'

      - name: Dump Kerbside API CA cert details
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'openssl s_client -showcerts -connect 10.0.2.3:13002'

      - name: Attempt to start an instance with a console and connect to it
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'cd /srv/shakenfist/kerbside-patches; sudo ./tools/test-console'

      - name: Dump netstat
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo netstat -plnt || true'

      - name: Dump haproxy docker logs
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo docker logs haproxy || true'

      - name: Dump kerbside API docker logs
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo docker logs kerbside_api || true'

      - name: Dump kerbside proxy docker logs
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo docker logs kerbside_proxy || true'

      - name: Inspect kerbside API docker container
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo docker inspect kerbside_api || true'

      - name: Inspect kerbside proxy docker container
        if: always()
        run: |
          . $GITHUB_ENV
          ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
              -o UserKnownHostsFile=/dev/null \
              ${{ matrix.test.baseuser }}@10.0.2.2 \
              'sudo docker inspect kerbside_proxy || true'

      - name: Collect logs
        if: always()
        run: |
          . $GITHUB_ENV
          . ci-environment.sh

          mkdir -p /tmp/bundle
          cp /etc/hosts /tmp/bundle/

          for node in ${nodes}; do
              echo
              echo
              node_address=${!node}
              echo "${node} is at ${node_address}"
              echo

              ssh -i /srv/github/id_ci -o StrictHostKeyChecking=no \
                  -o UserKnownHostsFile=/dev/null \
                  ${{ matrix.test.baseuser }}@${node_address} \
                  'cd /srv/shakenfist/kerbside-patches/tools; sudo ./gather-logs'
              scp -i /srv/github/id_ci -o StrictHostKeyChecking=no \
                  -o UserKnownHostsFile=/dev/null \
                  ${{ matrix.test.baseuser }}@${node_address}:/tmp/bundle.zip /tmp/bundle/${node}.zip

              mkdir -p /tmp/bundle/${node}
              cd /tmp/bundle/${node}
              unzip ../${node}.zip || true
              rm ../${node}.zip
          done

          echo
          echo
          echo "Building single bundle"
          echo
          cd /tmp/
          zip -r bundle.zip bundle


      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bundle-${{ matrix.test.name }}
          retention-days: 90
          if-no-files-found: error
          path: /tmp/bundle.zip